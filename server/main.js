import { Meteor } from 'meteor/meteor';
import '../imports/api/eventos.js';
//import style from "angular-material/angular-material.css";

Meteor.startup(() => {
  // code to run on server at startup
});
